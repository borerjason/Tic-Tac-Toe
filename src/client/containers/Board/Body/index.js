import styled from 'styled-components';

const Body = styled.div`
  display: flex;
  flex-wrap: wrap;
  width: 400px;
  height: 400px;
  border: 1px solid back;

 &:first-child {
   background-color: pink;
  }
`

export default Body;
